syntax = "proto3";

package ares.core;

import "StepTemplate.proto";

message ExperimentTemplate {
	repeated StepTemplate StepTemplates = 1; // Collection of step templates used to generate executable steps
}