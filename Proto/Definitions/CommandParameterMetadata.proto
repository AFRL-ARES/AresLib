syntax = "proto3";

package ares.core;

message CommandParameterMetadata {
	string Name = 1; // Name descriptor of the argument/parameter
	string Unit = 2; // Unit descriptor, if applicable, of the argument/parameter
	repeated float Constraints = 3; // Limits of the argument/parameter formatted to be compatible with planners
}