syntax = "proto3";

package ares.messaging;

message Limits {
	float minimum = 1; // Lower bound
	float maximum = 2; // Upper bound
	int64 index = 3; // Index of the collection in which executed this template
}