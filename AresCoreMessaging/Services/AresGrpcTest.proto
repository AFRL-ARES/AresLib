syntax = "proto3";

package ares.core.services;

import "CompletedExperiment.proto";
import "ExperimentTemplate.proto";

service TestExperimentRunner {
	rpc RunExperiment(ares.core.messages.ExperimentTemplate) returns (ares.core.messages.CompletedExperiment);
}