syntax = "proto3";

package ares.messaging;

import "google/protobuf/wrappers.proto";

message PlannerAdapterInfo {
	google.protobuf.StringValue unique_id = 1; // A unique id for this planner
	string adapter_name = 2; // name of the planner adapter
	string type = 3; // type name of the planner
	string version = 4; // version of the planner
	google.protobuf.StringValue address = 5; // address for communicating with the planner
}